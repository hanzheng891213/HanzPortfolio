# Cloudflare Pages 部署时需要在 Dashboard 设置以下环境变量：
# 1. Pages → Settings → Environment variables → 添加 JWT_SECRET
#    (生成方式: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
# 2. Pages → Settings → Functions → D1 database bindings → 绑定 DB 到 hanz-portfolio-db

# 本地开发用 — wrangler dev 会从 .dev.vars 读取
# JWT_SECRET=your_jwt_secret_here
